define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    return function () {
        return React.createElement('div', {}, React.createElement('div', {
            'className': 'f g-' + (1 + 2) + ' h-i' + ' ' + _.keys(_.pick({
                'a': true,
                b: false,
                c: 1,
                'd-e': true
            }, _.identity)).join(' ')
        }), '\n\tOrder should not matter\n\t', React.createElement('div', {
            'className': _.keys(_.pick({
                'a': true,
                b: false,
                c: 1,
                'd-e': true
            }, _.identity)).join(' ') + ' ' + 'f g-' + (1 + 2) + ' h-i'
        }), React.createElement('div', {}));
    };
});