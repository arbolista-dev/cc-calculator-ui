define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    return function () {
        return React.createElement('div', {}, '\n    Strings and execution mixing is:', 2 > 1 ? 'correct' : 'incorrect', '\n');
    };
});