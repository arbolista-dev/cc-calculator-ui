define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    function scopeVal1(squares, val) {
        var val = square.val;
        return React.createElement('div', {}, React.createElement('span', {}, squareIndex + 1), React.createElement('span', {}, val));
    }
    function scopeSquares2(squares) {
        var squares = [
            { val: 1 },
            { val: 4 },
            { val: 9 },
            { val: 16 }
        ];
        return React.createElement('div', {}, scopeVal1.apply(this, [
            squares,
            val
        ]));
    }
    return function () {
        return scopeSquares2.apply(this, [squares]);
    };
});