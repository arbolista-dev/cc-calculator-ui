define([
    'react/addons',
    'lodash'
], function (React, _) {
    'use strict';
    function scopeSections1() {
        var sections = [];
        return sections.length > 1 ? React.createElement('div', {}) : null;
    }
    return function () {
        return scopeSections1.apply(this, []);
    };
});